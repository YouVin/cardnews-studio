# 다른 사람에게 나눠주기

## 무엇을 주느냐부터 정한다

받는 사람에 따라 줄 게 다르다. 전부 주면 아무것도 안 쓴다.

| 받는 사람 | 주는 것 | 이유 |
|---|---|---|
| 디자이너 · 마케터 | `figma-plugin/` 폴더만 | 터미널을 안 쓴다 |
| 기획자 · 개발자 | 전체 폴더 | 클로드 코드 트랙까지 씀 |
| 클라이언트 | 아무것도 안 줌 | 결과물만 준다 |

## 방법 1 — 폴더 그대로 (가장 쉬움)

압축해서 보내고, 받는 사람이 피그마에서 `Import plugin from manifest…` 하면 끝.
설치가 아니라 폴더를 열어두는 방식이라 **폴더를 지우면 플러그인도 사라진다.**
바탕화면 말고 문서 폴더 같은 데 두라고 알려줘야 한다.

보내기 전에 지울 것:

```
input/          ← 클라이언트 자료가 들어 있다
docs/           ← 작업 결과물
specs/          ← 작업 결과물
```

`examples/library/` 도 확인해라. 클라이언트 비공개 내용이 섞였을 수 있다.

## 방법 2 — 깃허브

```bash
cd cardnews-to-prd
git init
git add .
git commit -m "카드뉴스 기획 스튜디오"
```

`.gitignore` 는 이미 포함돼 있다. `input/`, `docs/`, `specs/` 가 제외된다.

**퍼블릭으로 올릴 거면 커밋 전에 반드시 확인해라.**
한 번 올라간 건 지워도 커밋 기록에 남는다.

## 방법 3 — 피그마 커뮤니티 배포

`Plugins` → `Development` → 플러그인 우클릭 → `Publish`

- 심사가 있고 며칠 걸린다
- 누구나 검색해서 쓸 수 있다
- **API 키를 코드에 넣은 채로 올리면 안 된다.** 지금 구조는 사용자가 각자 넣게 되어 있으니 안전하다

회사 안에서만 쓸 거면 `Publish` 창에서 조직 전용으로 범위를 좁힐 수 있다.

## 받는 사람에게 알려줘야 할 것

한 번씩 다 걸리는 지점이다.

1. **피그마 데스크톱 앱**이어야 한다. 브라우저는 로컬 플러그인이 안 된다
2. 한글 폰트가 없으면 글자가 깨진다. Pretendard 설치를 권한다
3. 사진은 회색 영역에 **끌어다 놓으면** 채워진다
4. `직접 붙여넣기` 모드는 API 키가 필요 없다. 여기부터 써보라고 한다

## 쓰기 전에 손봐야 할 것

브랜드가 다르면 이것부터 바꾼다.

| 바꿀 것 | 위치 |
|---|---|
| 컬러·브랜드명·핸들 | 플러그인 설정 화면 |
| 카피 규칙·글자 수 | `prompts/E-style.md` |
| 톤 레퍼런스 | `examples/library/` |
| 카드 디자인 | `figma-plugin/code.js` 의 `renderDesign()` |

`examples/library/` 는 **자기 작업물로 갈아끼우는 게 제일 효과가 크다.**
잘 나온 `specs/studio.json` 을 3~5개 넣어두면 톤이 그쪽으로 붙는다.
그 이상 넣으면 오히려 흐려진다.
