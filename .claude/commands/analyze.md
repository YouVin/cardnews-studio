---
description: [트랙 A] 카드뉴스를 카드별로 해부합니다
argument-hint: [파일 경로 또는 비움]
---

`$ARGUMENTS` 를 읽어라. 비었으면 `input/` 폴더 전체를 파일명 순서대로 읽어라.

이건 요약이 아니다. **완성된 결과물을 보고 그 뒤의 판단을 복원하는 작업**이다.
"왜 이 카드가 여기 있는가", "왜 이 문장이 이 순서인가"를 읽어내라.

`prompts/A00-analyze.md`의 구조대로 `docs/A00-teardown.md`를 쓰고 멈춰라.

쓰고 나서 보고할 것:
- 결과물에서 확실히 읽어낸 것 vs 해석으로 채운 것
- 사용자에게 확인할 질문 3개 이하 (실제 기획 의도를 물어야 문서가 진짜가 된다)
