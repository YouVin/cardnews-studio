---
description: [트랙 B] 화면정의서 (화면별 상태·인터랙션·검증)
---

`docs/B02-ia.md` 와 `docs/B03-spec.md` 를 읽어라.

`prompts/B04-screendef.md`의 구조대로 `docs/B04-screendef.md`를 쓰고 멈춰라.

IA의 전체 페이지 목록에 있는 **모든 화면**을 빠짐없이 다뤄라.
각 화면마다 관련 기능 번호(B03의 1.1, 2.3 등)를 연결한다. 이 연결이 두 문서를 묶는다.

화면 상태 5종(기본·로딩·빈 상태·오류·권한 없음)을 모두 채운다.
"없음"이라고 쓰지 마라. 빈 화면은 분위기가 아니라 방향을 주는 자리다.

다 쓰고 다룬 화면 수 / IA의 전체 화면 수를 대조해서 보고하고 멈춰라.
