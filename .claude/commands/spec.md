---
description: [트랙 B] 기능명세서 (계층 트리 + 번호 체계)
---

`docs/B02-ia.md` 만 읽어라.

`prompts/B03-spec.md`의 구조대로 `docs/B03-spec.md`를 쓰고 멈춰라.

**이 문서는 화면 단위가 아니라 기능 단위다.** 대분류 → 중분류 → 소분류로 나누고
`1`, `1.1`, `1.1.1` 번호를 매긴다. 번호는 재사용하지 않는다.

화면별 상태·예외·검증규칙은 **여기 쓰지 마라.** 다음 단계(`/screendef`) 담당이다.
두 문서가 중복하면 나중에 한쪽만 고쳐져서 어긋난다.
