# 카드뉴스 → 기획 산출물 역구성

완성된 카드뉴스를 넣으면 그게 만들어지기까지의 **기획 문서를 역으로 복원**한다.
목적은 "이 결과물 뒤에 이런 판단이 있었다"를 제3자에게 증명하는 것이다.

## 두 개의 트랙

**트랙 A — 콘텐츠 기획**
`/analyze` → `/brief` → `/message` → `/layout`

**트랙 B — 웹 기획 역기획**
`/prd` → `/ia` → `/spec` → `/screendef` → `/flow` → `/wireframe`
트랙 A의 `docs/A01-brief.md` 를 입력으로 받는다.

**피그마 연결**
`/studio [카드수]` — 트랙 A 문서를 읽어 `specs/studio.json` 을 만든다.
이 JSON을 피그마 플러그인의 `직접 붙여넣기` 모드에 넣으면 기획 보드·와이어프레임·카드 시안이 나온다.
스키마는 `schemas/studio.schema.json`. 한 글자도 벗어나면 플러그인이 파싱에 실패한다.

**묶기**
`/package [A|B|AB]` — 전체 문서를 읽어 `docs/00-summary.md` (한 장 요약 + 제출 가이드)를 만든다.
모든 문서가 끝난 뒤 마지막에 한 번 실행한다.

## 절대 규칙

1. **지어내지 않는다.** 이 문서들은 "실제로 이렇게 기획했다"는 주장으로 쓰인다.
   근거 없는 수치(도달률, 전환율, 참여율)는 질문 한 번에 무너진다.
   근거 표시: `[카드뉴스]` `[메모]` `[추정]` — 대괄호 방식으로만 통일. HTML 주석 금지
2. **[추정]을 숨기지 않는다.** 드러내는 편이 신뢰를 준다
3. **단계가 끝나면 멈춘다.** 파일을 쓰고 사용자 확인을 기다린다
4. **각 단계는 앞 단계 산출물만 읽는다.** 예외: `/layout`은 원본 이미지를 다시 본다
5. **B03과 B04는 내용을 중복하지 않는다.**
   기능명세서(B03)는 무엇을 만드는가, 화면정의서(B04)는 화면에서 어떻게 보이는가
6. 문서는 한국어. 형용사와 감탄사를 쓰지 않는다. 판단과 근거만 쓴다

## ID 규칙

- 화면 ID `S01`, `S02` — B02에서 확정. 이후 모든 문서와 JSON에서 동일 유지
- 기능 번호 `1`, `1.1`, `1.1.1` — B03에서 확정. 재사용 금지, 빠져도 비워둔다
- 그룹명 — B02에서 확정. 사이트맵 subgraph, 페이지 목록, screens.json의 `group` 세 곳이 일치

## 입력 자료 구분

`input/` 안의 파일은 두 종류다.

- **카드뉴스** (숫자 파일명 이미지) — 공개된 완성 결과물
- **보조 자료** (`notes-` 로 시작) — 사용자가 가진 원본 맥락. 이게 더 정확하다

충돌하면 보조 자료를 따른다.
카드뉴스에만 있는 수치·주장은 마케팅 표현일 수 있으니 지표로 옮기지 않는다.

## 산출물

```
docs/A00-teardown.md    카드별 해부
docs/A01-brief.md       콘텐츠 기획서
docs/A02-message.md     메시지 설계서
specs/cards.json        카드 레이아웃 → 피그마

docs/B01-prd.md         제품 요구사항 정의서
docs/B02-ia.md          사이트맵 + 전체 페이지 목록
docs/B03-spec.md        기능명세서 (계층 트리)
docs/B04-screendef.md   화면정의서 (화면별 상태·인터랙션)
docs/B05-flow.md        유저 플로우
specs/screens.json      웹 화면 레이아웃 → 피그마

specs/studio.json       피그마 플러그인 입력 (JSON 그대로 붙여넣기)
docs/00-summary.md      제출용 한 장 요약 (마지막에 /package 로 생성)
```
